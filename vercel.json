{
  "version": 2,
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/avatars/(.*)", "dest": "/avatars/$1" },
    { "src": "/photos/(.*)", "dest": "/photos/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
